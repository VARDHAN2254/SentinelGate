spring:
  datasource:
    url: jdbc:h2:mem:sentinelgate_local;DB_CLOSE_DELAY=-1;MODE=PostgreSQL
    username: sa
    password: 
    driver-class-name: org.h2.Driver
  jpa:
    database-platform: org.hibernate.dialect.H2Dialect
    hibernate:
      ddl-auto: update
  data:
    redis:
      host: localhost
      port: 6379

sentinelgate:
  admin:
    username: admin
    password: AdminSecret123!
  demo:
    seed-events: true
